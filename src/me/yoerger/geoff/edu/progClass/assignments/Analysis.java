package me.yoerger.geoff.edu.progClass.assignments;

/**
 * Used to mark than an assignment has questions that go with it.
 * @author yoerggeo000
 * @since Module 8
 * 
 */
public interface Analysis {
	
	/**
	 * Print all those questions
	 * @param printer The printer to print to so this method doesnt have to define one.
	 */
	public void printQuestions(Printer printer);
	
}
